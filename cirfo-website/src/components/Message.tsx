// older project would use js based components, I'll use function based components

function Message(){
    const title = 'Hello World!... by Cirfo'
    return <h1>{title}</h1>; // JavaScript XML (JSX) -> JS after compilation!
}

export default Message;